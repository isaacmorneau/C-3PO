#include <stdbool.h>
#include <stdio.h>
int main(int argc, char **argv) {
    (void)argv;

    {
        volatile bool assert_check =!(argc > 0);
        if (assert_check) {

        __asm__(
            ".byte 0x66;"
            ".byte 0xf;"
            ".byte 0xe7;"
            :::);
        }
    }    for (int i = 0; i < 10; ++i) {
        printf("%d", i);
    }
}
